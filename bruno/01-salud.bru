meta {
  name: 01 · Salud del servicio
  type: http
  seq: 1
}

get {
  url: {{base_url}}/health
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.ok: eq true
}

docs {
  Healthcheck (el mismo que usa Railway). Sin autenticación.
}
